# [!] Note for BungeeCord usage.
# The only configuration sections that you can modify are 'prefix', 'announces-delay' and 'titles', per the moment I'm working
# to separate this and make it more ordered to avoid confusions.
config:
  # Plugin Messages Prefix
  prefix: "&6[XStream]"

  # Announces Delay Time.
  # In ticks. 20 Ticks -> 1 second.
  announces-delay: 100

  sounds:
    # Sounds ID's
    no-perm: ENTITY_ITEM_BREAK
    reload: UI_BUTTON_CLICK
    live: ENTITY_EXPERIENCE_ORB_PICKUP

    # Sounds Volume
    volume-level: 1

  titles:
    # Allow the titles?
    allow: true

    # Title Times Parameters (In ticks).
    # In-Coming time.
    fade-in: 20

    # Staying time.
    stay: 60

    # Outing time.
    fade-out: 20

  live:
    # Title for the Live Manager Menu.
    menu-title: "&7Manager -> Live"

    # Rows amount for the menu.
    rows: 3

    # Insert Live button.
    announce-live:
      # Display Name.
      display-name: "&8&l> &eAnnounce Stream "

      # Item Lore.
      lore: |-
        &7To click the item, you announces the stream to all!

    # Usage button.
    usage:
      # Display Name.
      display-name: "&8&l> &bUsage"

      # Item Lore.
      lore: |-
        &7To send the live press the &aAnnounce Live &7button.
        &8[&c!&8] &fYou need before insert the stream link.
        &l
        &7If you select the &cClose Menu &7button, will be close
        &7the &cLive Manager &7menu.

    # Offline Button.
    offline:
      # Display Name.
      display-name: "&8&l> &cFinalize Stream"

      # Item Lore.
      lore: |-
        &7If you want end the stream click this button.

    # Close button.
    close-menu:
      # Display Name.
      display-name: "&8&l> &cClose Live Manager"

      # Item Lore.
      lore: |-
        &7Do you want close the &cLive Manager&7?
